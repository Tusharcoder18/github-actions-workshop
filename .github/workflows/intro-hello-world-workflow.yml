name: Intro - Hello World Workflow

on:
    workflow_dispatch:

jobs:
    run:
        runs-on: ubuntu-latest
        steps:
            - name: Say Hello to the World
              run: echo "Hello World!"